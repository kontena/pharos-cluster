language: ruby
sudo: required
cache: bundler
bundler_args: --without development
env:
  global:
    - GITHUB_TOKEN=xxx
stages:
  - name: publish binary
jobs:
  include:
    - stage: publish binary
      script: ./build/travis/macos.sh
      rvm: 2.4
      os: osx