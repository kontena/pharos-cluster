apiVersion: v1
kind: ConfigMap
metadata:
  name: local-provisioner-config
  namespace: kube-system
data:
  storageClassMap: |
    fast-disks:
       hostDir: /storage
       mountDir: /storage
       blockCleanerCommand:
         - "/scripts/shred.sh"
         - "2"