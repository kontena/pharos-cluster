kind: ConfigMap
apiVersion: v1
metadata:
  name: nginx-configuration
  namespace: ingress-nginx
  labels:
    app: ingress-nginx
data:
  <% config.configmap.to_h.each do |k, v| %>
  <%= k %>: "<%= v %>"
  <% end %>
