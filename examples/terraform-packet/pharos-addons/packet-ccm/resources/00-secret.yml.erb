apiVersion: v1
kind: Secret
metadata:
  name: packet-cloud-config
  namespace: kube-system
data:
  apiKey: "<%= Base64.strict_encode64(config.api_key) %>"
  projectID: "<%= Base64.strict_encode64(config.project_id) %>"